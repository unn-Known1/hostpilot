{
  "name": "hostpilot",
  "version": "2.0.0",
  "description": "A powerful, beautiful cross-platform GUI for managing hosts file with enterprise-grade features",
  "main": "src/main/main.cjs",
  "scripts": {
    "dev": "concurrently \"npm run dev:vite\" \"npm run dev:electron\"",
    "dev:vite": "vite",
    "dev:electron": "wait-on http://localhost:5173 && electron .",
    "build:react": "vite build",
    "build": "npm run build:react && electron-builder",
    "build:win": "npm run build:react && electron-builder --win",
    "build:mac": "npm run build:react && electron-builder --mac",
    "build:linux": "npm run build:react && electron-builder --linux"
  },
  "author": "HostPilot",
  "license": "MIT",
  "devDependencies": {
    "@tailwindcss/postcss": "^4.0.0",
    "@vitejs/plugin-react": "^4.2.1",
    "autoprefixer": "^10.4.17",
    "concurrently": "^8.2.2",
    "electron": "^28.2.0",
    "electron-builder": "^24.13.3",
    "postcss": "^8.4.35",
    "tailwindcss": "^4.0.0",
    "vite": "^5.1.4",
    "wait-on": "^7.2.0"
  },
  "dependencies": {
    "electron-log": "^5.1.1",
    "lucide-react": "^0.344.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "build": {
    "appId": "com.hostpilot.app",
    "productName": "HostPilot",
    "directories": {"output": "release"},
    "files": ["dist/**/*", "src/main/**/*"],
    "mac": {"category": "public.app-category.developer-tools", "target": ["dmg"]},
    "win": {"target": ["portable"]},
    "linux": {"target": ["AppImage"]}
  }
}
